import { UploadFileDirectiveDirective } from './upload-file-directive.directive';

describe('UploadFileDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadFileDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
